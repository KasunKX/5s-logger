import type { Metadata } from "next";
import { Demo } from "./Demo";
export const metadata:Metadata={title:"SiteSight — Visual 5S intelligence",description:"Turn workplace video into reviewable 5S observations, evidence and action."};
export default function Home(){return <Demo/>}
